import React from 'react';
import { SideBar } from "./SideBar";

export const Main: React.FC = () => {
    return (
        <div>
            <div>
                <SideBar />
            </div>
        </div>
    );
};
