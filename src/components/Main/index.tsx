import React from 'react';
import {SideBar} from "./SideBar";

const Main: React.FC = () => {
    return (
        <div>
            <div>
                <SideBar />
            </div>
        </div>
    );
};

export default Main;